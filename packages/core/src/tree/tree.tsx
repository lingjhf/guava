import { ComponentProps, } from '../types'

export interface TreeProps extends ComponentProps<HTMLDivElement> {
  data: []
  nodeKey: string
}

export const Tree = () => {

  return (
    <div></div>
  )
}